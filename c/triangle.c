#include <stdio.h>
	
int main(void)
{	int width, height;
	double area = 1 / 2 * width * height;
	scanf("%d", &width);
	scanf("%d", &heith);
	
	double area = 1 / 2 * width * height;

	printf("area: %/1f\n", area);

	return 0;
}
